import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';
class Recived_screen extends StatelessWidget {
  const Recived_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
